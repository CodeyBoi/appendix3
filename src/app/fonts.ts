import localFont from 'next/font/local';

export const bahnschrift = localFont({
  src: './bahnschrift.woff2',
  variable: '--font-bahnschrift',
});

export const castelar = localFont({
  src: './castelar.woff2',
  variable: '--font-castelar',
});
